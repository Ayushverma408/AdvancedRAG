# PostgreSQL connection string
DATABASE_URL="postgresql://postgres:password@localhost:5432/ragdb"

# Secret for signing JWTs (generate with: openssl rand -base64 32)
JWT_SECRET="change-me-to-a-long-random-string"

# URL where Chainlit is running (proxied via /chat)
CHAINLIT_URL="http://localhost:8000"

# Public-facing URL of this web app (used for redirects)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
